# Copia a android/keystore.properties (NO se versiona) y rellena tras crear el keystore:
#   keytool -genkey -v -keystore futbolismo-upload.jks -alias upload \
#     -keyalg RSA -keysize 2048 -validity 10000
storeFile=../futbolismo-upload.jks
storePassword=CAMBIA_ESTO
keyAlias=upload
keyPassword=CAMBIA_ESTO
